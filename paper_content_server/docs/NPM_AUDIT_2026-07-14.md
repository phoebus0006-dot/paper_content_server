# NPM Audit Snapshot — 2026-07-14

## Production dependencies (`npm audit --omit=dev`)

```
found 0 vulnerabilities
```

## All dependencies (`npm audit`, includes devDependencies)

3 moderate vulnerabilities, all transitive via `aedes` (dev-only MQTT broker used in r6 tests):

- `uuid <11.1.1` — Missing buffer bounds check in v3/v5/v6 (GHSA-w5hq-g745-h8pq)
  - Paths: `aedes → hyperid → uuid`, `aedes → uuid`
- `aedes 0.6.0 - 1.0.2` — depends on vulnerable `hyperid` and `uuid`
  - Path: `aedes` (top-level devDependency)
- `hyperid <=1.2.0 || 1.3.1 - 3.3.0` — depends on vulnerable `uuid`
  - Path: `aedes → hyperid`

## Assessment

- **Production risk: NONE.** All 3 vulnerabilities are in `aedes` (test-only MQTT broker library).
- `aedes` is only loaded by `test/r6/mqtt-real-broker-test.js` during the test suite — never in production runtime.
- **Fix:** `npm audit fix --force` would upgrade `aedes` to 1.1.1 (breaking change). Deferred until test migration can be validated.
- No action required for production deployment.

## Reproduction

```bash
cd paper_content_server
npm audit --omit=dev   # 0 vulnerabilities (production)
npm audit              # 3 moderate (dev-only, via aedes)
```
